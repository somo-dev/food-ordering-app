import { createSlice } from "@reduxjs/toolkit";

const cartItems= createSlice({
      name: "cart-items",
      initialState: {
      items: [],
      totalQuantity: 0,
      totalAmount: 0
},
reducers: {

}}
)